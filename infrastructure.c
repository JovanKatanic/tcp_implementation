#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include "state_machine.h"






int main() {
    //create new interface
    struct interface interface = create_interface("tun0");

    //create a cm

    //bind first ports
    
    //create a threads that calls accept

    //join threads

    return 0;
}